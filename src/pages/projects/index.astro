---
import { getCollection } from "astro:content"

const projects = await getCollection("projects")
---

<div class="">
  {projects.map((project) => (<div>{project.data.title} <ul>{project.data.tech.map((tech) => (<li>{tech}</li>))}</li></ul></div>))}
  
</div>